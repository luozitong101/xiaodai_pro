<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="lzt.xiaodai.cn.mapper.TProjectMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="lzt.xiaodai.cn.entity.TProject">
        <id column="id" property="id" />
        <result column="infoid" property="infoid" />
        <result column="authbankid" property="authbankid" />
        <result column="authmobileid" property="authmobileid" />
        <result column="identityid" property="identityid" />
        <result column="registerid" property="registerid" />
        <result column="itemid" property="itemid" />
        <result column="extends_1" property="extends1" />
        <result column="auditid" property="auditid" />
        <result column="mobile" property="mobile" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, infoid, authbankid, authmobileid, identityid, registerid, itemid, extends_1, auditid, mobile
    </sql>

</mapper>
